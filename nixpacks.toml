# Nixpacks configuration for Railway deployment
# Installs both Node.js and Python 3.11

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.pipInstall]
dependsOn = ["setup"]
cmds = ["pip install requests"]
